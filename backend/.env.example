# Elvanto API Configuration
# Note: API key is now entered by users in the application interface
# This file is optional and only needed if you want to set default API URL
ELVANTO_API_URL=https://api.elvanto.com/v1

# Database Configuration
DATABASE_URL=sqlite:///./data/elvanto.db

# Environment
ENV=development

